# Database
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/animalession_store
POSTGRES_PASSWORD=your_password

# Auth (generate with: openssl rand -base64 32)
AUTH_SECRET=your_auth_secret_here
AUTH_URL=http://localhost:3000

# Anthropic (for AI artifact generation)
# Supports both API keys (sk-ant-api03-...) and Claude Max setup-tokens (sk-ant-oat01-...)
# Generate a setup-token with: claude setup-token
ANTHROPIC_API_KEY=sk-ant-your-key-here
